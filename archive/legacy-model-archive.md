# Legacy Model Archive Note

This file replaces the old `models/README.md` after the repository was reorganized around `portfolio/`.

The old structure grouped files by type:

- `models/source-cad/`
- `models/stl-exports/`
- `models/assemblies/`
- `images/`
- `projects/`

The new structure groups files by project. Use [../portfolio/](../portfolio/) as the primary entry point.

Legacy manifests are preserved in [manifests/](manifests/) for reference.
